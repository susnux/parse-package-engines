{
  "name": "parse-package-engines",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub action for parsing the engine and package manager from a package.json",
  "keywords": [
    "actions",
    "package.json"
  ],
  "homepage": "https://github.com/nextcloud-libraries/parse-package-engines-action#readme",
  "bugs": {
    "url": "https://github.com/nextcloud-libraries/parse-package-engines-action/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nextcloud-libraries/parse-package-engines-action.git"
  },
  "license": "MIT",
  "author": "Nextcloud GmbH and Nextcloud contributors",
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "all": "npm run lint && npm run test && npm run coverage && npm run build",
    "build": "rolldown -c rolldown.config.js",
    "coverage": "npm run test:coverage && coverage-badges --output ./badges/coverage.svg",
    "lint": "eslint",
    "lint:fix": "eslint --fix",
    "local-action": "@github/local-action . src/main.js .env",
    "test": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@actions/core": "^1.11.1"
  },
  "devDependencies": {
    "@github/local-action": "^6.0.2",
    "@nextcloud/eslint-config": "^9.0.0-rc.5",
    "@vitest/coverage-v8": "^3.2.4",
    "builtin-modules": "^5.0.0",
    "coverage-badges-cli": "^2.2.0",
    "eslint": "^9.39.0",
    "make-coverage-badge": "^1.2.0",
    "rolldown": "^1.0.0-beta.46",
    "typescript": "^5.9.3",
    "vitest": "^4.0.7"
  },
  "engines": {
    "node": "^22 || >= 24"
  }
}
